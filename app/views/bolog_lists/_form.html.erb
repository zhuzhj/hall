<%= form_for(@bolog_list) do |f| %>
  <% if @bolog_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bolog_list.errors.count, "error") %> prohibited this bolog_list from being saved:</h2>

      <ul>
      <% @bolog_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
